# Copy this file to .env and fill in your values
# .env is gitignored — never commit real keys

# Anthropic Claude API (needed for Checkpoint 4 — sentence cleanup)
ANTHROPIC_API_KEY=your_api_key_here

# Optional: set to override config file camera index
CAMERA_INDEX=0
